"use client";

export default function Navbar() {
  return (
    <div className="w-full px-6 py-4 bg-white/10 backdrop-blur-md border-b" style={{ backgroundColor: `var(--color-navy)`, borderColor: `transparent` }}>
      {/* Placeholder, nanti bisa ditambah notif / profile */}
      <h2 className="text-lg font-semibold text-white">Welcome Back!</h2>
    </div>
  );
}